1. "scout" agent (outputFile: context.md): find all code relevant to: $@
2. "planner" agent (reads: context.md, outputFile: plan.md): create an implementation plan for "$@" using {previous}

Do NOT implement — just return the plan.
